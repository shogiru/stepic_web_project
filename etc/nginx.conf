  server {  
    listen 80 default;
                           
    location  ^~ /uploads/ {
      alias  /home/box/web/uploads;
    }

    location ~* ^.+\.\w+$ { 
      root   /home/box/web/public
    }

    location / {
      proxy_pass http://127.0.0.1:8000;
    }
  }
